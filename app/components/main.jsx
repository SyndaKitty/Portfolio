import React from 'react';
import Nav from 'Nav';
import Weather from 'Weather';

export default class Main extends React.Component {
  render() {
    return (
      <div>
        <Weather></Weather>
      </div>
    );
  }
}